info:
  name: TarifProStunde <= 5
  type: http
  seq: 6

http:
  method: GET
  url: "{{restUrl}}?tarifProStunde=5"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: tarifProStunde
      value: "5"
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers['content-type']
      operator: matches
      value: ^application\/json(; charset=utf-8)?$
    - expression: res.body.content
      operator: isArray
    - expression: res.body.content
      operator: isNotEmpty

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
